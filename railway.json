{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "bash -lc '/app/.venv/bin/python -m uvicorn webhook_api:app --host 0.0.0.0 --port ${PORT}'",
    "healthcheckPath": "/health"
  }
}
